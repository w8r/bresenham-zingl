export type setPixelFn = (x: number, y: number) => unknown;
export type setPixelAlphaFn = (x: number, y: number, alpha: number) => unknown;
export type setHLineFn = (x0: number, x1: number, y: number) => unknown;
